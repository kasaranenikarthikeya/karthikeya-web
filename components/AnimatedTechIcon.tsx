// This component is not used in the new design based on the provided image.
// It can be removed from the project.

import React from 'react';

const AnimatedTechIcon: React.FC<React.SVGProps<SVGSVGElement>> = (props) => (
  // Original SVG content here, returning null as it's deprecated.
  // This is to prevent errors if it's still imported somewhere during transition.
  null
);

export default AnimatedTechIcon;
